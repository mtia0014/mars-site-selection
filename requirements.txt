# MARS 选址系统 —— 核心依赖（跑通 Gradio 界面 + API 的最小集合）
# 安装：pip install -r requirements.txt
# 建议使用 Python 3.11 或 3.12（3.14 太新，部分科学计算库暂无 wheel）

pandas>=2.0
numpy>=1.24
requests>=2.28
openai>=1.30          # DeepSeek 兼容客户端
gradio>=4.0           # Web 界面
fastapi>=0.110        # API 服务
uvicorn[standard]>=0.27
pydantic>=2.0
python-dotenv>=1.0    # 从 .env 读取 API Key
